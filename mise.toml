[env]
_.python.venv = { path = ".venv", create = true }

[tools]
python = "3.14.5"
uv = "0.11.15"

[tasks.install]
description = "Install deps"
alias = "i"
run = "uv pip install ansible  ansible-lint pre-commit detect-secrets"
